{
  "name": "family-tree-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend:install": "cd frontend && npm install",
    "backend:install": "cd backend && npm install",
    "install:all": "npm run frontend:install && npm run backend:install",
    "frontend:dev": "cd frontend && npm run dev",
    "backend:dev": "cd backend && DYNAMODB_ENDPOINT=http://localhost:8000 AWS_REGION=us-east-1 TABLE_NAME=FamilyTreeTable node local.js",
    "dev": "concurrently \"npm run backend:dev\" \"npm run frontend:dev\"",
    "infra:init": "cd infrastructure && terraform init",
    "infra:plan": "cd infrastructure && terraform plan",
    "infra:apply": "cd infrastructure && terraform apply"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
